---

awscli_python_version: 3
awscli_packages:
  2:
    - python-pip
    - python-setuptools
  3:
    - python3-pip
    - python-setuptools
    - python3-setuptools

awscli_pip_binary:
  2: pip
  3: pip3

awscli_pip_packages:
  - awscli

awscli_custom_pip_packages: []
awscli_plugins_params: []
awscli_default_output_format: json
awscli_default_region: local

awscli_profiles:
  - name: default
    secret_access_key: "SomeSecretAccessKey"
    access_key_id: "SomeAccessKeyID"
